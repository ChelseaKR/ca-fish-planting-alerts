# ca-fish-planting-alerts (working name)

California trout-planting alerts and per-water stocking history, built from the
California Department of Fish and Wildlife's weekly planting schedule. No
account, no tracking.

[moved to private strategy notes]

## Shape

- `pipeline/` — Python. Polls CDFW's weekly schedule, keeps a per-water history
  (this is the asset: nobody else keeps it), and publishes a versioned snapshot
  as one static file. Nightly; no human step.
- `schema/` — the snapshot contract the app consumes. Owned by the pipeline.
- `site/` — static HTML, generated by the pipeline. One page per water: what was
  planted and when, a subscribable `.ics`, and where the app is. This is the
  free discovery surface.
- `ios/` — SwiftUI. Favourite waters, background refresh of the snapshot, a
  local notification when a favourite appears in the new week. Paid up front.
  No server, no push service, no account.
- `docs/` — decisions, licences and attributions.

## Not yet decided

The name and the domain.
